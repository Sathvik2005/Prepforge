# System Architecture - Interview Preparation Platform

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Resume     │  │  Interview   │  │   Progress   │          │
│  │   Upload     │  │   Lobby      │  │   Tracker    │          │
│  │   Component  │  │  Component   │  │  Component   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                   │
│                            │                                       │
│                       Axios HTTP                                  │
└────────────────────────────┼──────────────────────────────────────┘
                             │
┌────────────────────────────┼──────────────────────────────────────┐
│                    EXPRESS.JS SERVER                              │
│                            │                                       │
│  ┌─────────────────────────┴──────────────────────────┐          │
│  │                   API ROUTES                        │          │
│  ├────────────┬──────────────────┬─────────────────────┤          │
│  │ /interview-│ /interview/      │ /evaluation/        │          │
│  │   prep/*   │ conversational/* │    (future)         │          │
│  └─────┬──────┴────────┬─────────┴──────────┬──────────┘          │
│        │               │                    │                     │
│        │               │                    │                     │
│  ┌─────┴──────┐  ┌────┴────────┐  ┌────────┴─────────┐          │
│  │  Resume    │  │  Interview  │  │   Evaluation     │          │
│  │  Parser    │  │   Engine    │  │    Engine        │          │
│  │  Service   │  │   Service   │  │   (future)       │          │
│  └─────┬──────┘  └────┬────────┘  └────────┬─────────┘          │
│        │               │                    │                     │
│        │               │                    │                     │
│  ┌─────┴──────┐  ┌────┴────────┐  ┌────────┴─────────┐          │
│  │ JD Matcher │  │   OpenAI    │  │   Report Gen     │          │
│  │  Service   │  │  GPT-4 API  │  │   (future)       │          │
│  └─────┬──────┘  └────┬────────┘  └────────┬─────────┘          │
│        │               │                    │                     │
└────────┼───────────────┼────────────────────┼─────────────────────┘
         │               │                    │
         │          (Phrasing Only)           │
         │                                    │
┌────────┴────────────────────────────────────┴─────────────────────┐
│                   MONGODB DATABASE                                │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────────┐          │
│  │  Parsed    │  │Conversational│  │  Skill Gaps     │          │
│  │  Resumes   │  │ Interviews   │  │   (future)      │          │
│  └────────────┘  └──────────────┘  └─────────────────┘          │
│                                                                    │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────────┐          │
│  │ Interview  │  │   Reports    │  │    Users        │          │
│  │ Progress   │  │  (future)    │  │  (existing)     │          │
│  └────────────┘  └──────────────┘  └─────────────────┘          │
└────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### 1. Resume Upload & ATS Scoring Flow

```
┌──────────┐
│  User    │
│ Uploads  │
│ Resume   │
└────┬─────┘
     │ PDF/DOC file
     ▼
┌────────────────────┐
│ ResumeUpload.jsx   │
│ (Frontend)         │
│ - Drag & Drop UI   │
│ - File Validation  │
└────┬───────────────┘
     │ FormData (multipart/form-data)
     │ POST /api/interview-prep/upload-resume
     ▼
┌────────────────────────┐
│ interviewPrep.js       │
│ (Route Handler)        │
│ - Multer File Upload   │
└────┬───────────────────┘
     │ File Buffer + Metadata
     ▼
┌────────────────────────────────┐
│ resumeParserService.js         │
│ ┌──────────────────────────┐  │
│ │ 1. Extract Text          │  │
│ │    - pdf-parse (PDF)     │  │
│ │    - mammoth (DOC)       │  │
│ └──────────────────────────┘  │
│ ┌──────────────────────────┐  │
│ │ 2. Parse Sections        │  │
│ │    - Regex Detection     │  │
│ │    - Keyword Extraction  │  │
│ └──────────────────────────┘  │
│ ┌──────────────────────────┐  │
│ │ 3. Extract Skills        │  │
│ │    - Dictionary Matching │  │
│ │    - Categorization      │  │
│ └──────────────────────────┘  │
│ ┌──────────────────────────┐  │
│ │ 4. Calculate ATS Score   │  │
│ │    Formula:              │  │
│ │    Skills × 0.30         │  │
│ │    Experience × 0.25     │  │
│ │    Education × 0.20      │  │
│ │    Structure × 0.15      │  │
│ │    Keywords × 0.10       │  │
│ └──────────────────────────┘  │
└────┬───────────────────────────┘
     │ Parsed Resume Object
     ▼
┌────────────────────┐
│ ParsedResume Model │
│ (MongoDB)          │
│ - Save to DB       │
└────┬───────────────┘
     │ Resume ID + ATS Score
     ▼
┌────────────────────┐
│ Frontend Display   │
│ - Total Score      │
│ - Component Scores │
│ - Explanations     │
└────────────────────┘
```

### 2. Conversational Interview Flow

```
┌──────────┐
│  User    │
│ Starts   │
│Interview │
└────┬─────┘
     │ POST /api/interview/conversational/start
     │ { userId, interviewType, targetRole, resumeId }
     ▼
┌─────────────────────────────────┐
│ conversationalInterview.js      │
│ (Route Handler)                 │
└────┬────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────┐
│ interviewEngineService.js                │
│ ┌────────────────────────────────────┐  │
│ │ 1. Select Question Pool            │  │
│ │    - HR / Technical / Behavioral   │  │
│ │    - Filter by difficulty          │  │
│ │    - Select topic (diverse)        │  │
│ └────────────────────────────────────┘  │
│ ┌────────────────────────────────────┐  │
│ │ 2. Personalize Question            │  │
│ │    - GPT-4 API (phrasing only)     │  │
│ │    - Context: targetRole, resume   │  │
│ └────────────────────────────────────┘  │
└────┬─────────────────────────────────────┘
     │ First Question
     ▼
┌────────────────────┐
│ User sees question │
│ Types answer       │
└────┬───────────────┘
     │ POST /api/interview/conversational/:id/answer
     │ { answer: { text, timeSpent } }
     ▼
┌─────────────────────────────────────────────┐
│ ConversationalInterview Model               │
│ ┌───────────────────────────────────────┐  │
│ │ evaluateTurn() Method                 │  │
│ │ ┌─────────────────────────────────┐  │  │
│ │ │ 1. Clarity Score                │  │  │
│ │ │    - Word count (30-200 optimal)│  │  │
│ │ └─────────────────────────────────┘  │  │
│ │ ┌─────────────────────────────────┐  │  │
│ │ │ 2. Relevance Score              │  │  │
│ │ │    - Keyword matching           │  │  │
│ │ │    - Against expectedKeyPoints  │  │  │
│ │ └─────────────────────────────────┘  │  │
│ │ ┌─────────────────────────────────┐  │  │
│ │ │ 3. Depth Score                  │  │  │
│ │ │    - Regex: /for example/       │  │  │
│ │ │    - Regex: /because|reason/    │  │  │
│ │ └─────────────────────────────────┘  │  │
│ │ ┌─────────────────────────────────┐  │  │
│ │ │ 4. Structure Score              │  │  │
│ │ │    - Sentence count             │  │  │
│ │ │    - Intro/conclusion detection │  │  │
│ │ └─────────────────────────────────┘  │  │
│ │ ┌─────────────────────────────────┐  │  │
│ │ │ 5. Confidence Score             │  │  │
│ │ │    - Hedging words penalty      │  │  │
│ │ │    - /i think|maybe|perhaps/    │  │  │
│ │ └─────────────────────────────────┘  │  │
│ │ ┌─────────────────────────────────┐  │  │
│ │ │ 6. Weighted Turn Score          │  │  │
│ │ │    = clarity×0.20 +             │  │  │
│ │ │      relevance×0.25 +           │  │  │
│ │ │      depth×0.25 +               │  │  │
│ │ │      structure×0.15 +           │  │  │
│ │ │      confidence×0.15            │  │  │
│ │ └─────────────────────────────────┘  │  │
│ └───────────────────────────────────────┘  │
└────┬────────────────────────────────────────┘
     │ Evaluation Result
     ▼
┌────────────────────────────────┐
│ Check Follow-up Trigger        │
│ IF relevance < 50 OR depth < 60│
└────┬──────────────┬────────────┘
     │ Yes          │ No
     ▼              ▼
┌──────────────┐  ┌─────────────────┐
│ Generate     │  │ Generate Next   │
│ Follow-up    │  │ New Question    │
│ (GPT-4)      │  │ (Question Pool) │
└──────┬───────┘  └────┬────────────┘
       │               │
       └───────┬───────┘
               ▼
┌──────────────────────────┐
│ Return to User:          │
│ - Evaluation Scores      │
│ - Feedback               │
│ - Next Question          │
└──────────────────────────┘
```

### 3. Resume-JD Matching Flow

```
┌──────────┐
│  User    │
│ Provides │
│   JD     │
└────┬─────┘
     │ POST /api/interview-prep/resume/:id/match-jd
     │ { jobDescription: "..." }
     ▼
┌───────────────────────────────────┐
│ jdMatcherService.js               │
│ ┌─────────────────────────────┐  │
│ │ 1. Parse Job Description    │  │
│ │    - Extract required skills│  │
│ │    - Extract preferred      │  │
│ │    - Detect experience req  │  │
│ │    - Detect education req   │  │
│ └─────────────────────────────┘  │
│ ┌─────────────────────────────┐  │
│ │ 2. Load Resume Skills       │  │
│ │    - Get from ParsedResume  │  │
│ │    - Flatten categories     │  │
│ └─────────────────────────────┘  │
│ ┌─────────────────────────────┐  │
│ │ 3. Calculate Match          │  │
│ │    Formula:                 │  │
│ │    match% = (matched/total) │  │
│ │    where:                   │  │
│ │    total = req×1.0 +        │  │
│ │            pref×0.5          │  │
│ │    matched = matched_req×1.0│  │
│ │            + matched_pref×0.5│  │
│ └─────────────────────────────┘  │
│ ┌─────────────────────────────┐  │
│ │ 4. Identify Gaps            │  │
│ │    - Missing required       │  │
│ │    - Missing preferred      │  │
│ └─────────────────────────────┘  │
│ ┌─────────────────────────────┐  │
│ │ 5. Generate Recommendations │  │
│ │    - Priority by severity   │  │
│ │    - Actionable advice      │  │
│ └─────────────────────────────┘  │
└────┬──────────────────────────────┘
     │ Match Result
     ▼
┌────────────────────────┐
│ Frontend Display:      │
│ - Match Percentage     │
│ - Matched Skills       │
│ - Missing Skills       │
│ - Recommendations      │
└────────────────────────┘
```

---

## 🔄 Evaluation Pipeline

### Turn-by-Turn Evaluation Process

```
User Answer (Text)
       │
       ├──────────────────────┐
       │                      │
       ▼                      ▼
┌──────────────┐      ┌──────────────┐
│  Word Count  │      │   Tokenize   │
│   Analysis   │      │   Extract    │
└──────┬───────┘      └──────┬───────┘
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│   Clarity    │      │  Keyword     │
│   Score      │      │  Matching    │
│   (20%)      │      └──────┬───────┘
└──────┬───────┘             │
       │                     ▼
       │              ┌──────────────┐
       │              │  Relevance   │
       │              │   Score      │
       │              │   (25%)      │
       │              └──────┬───────┘
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│   Regex      │      │  Sentence    │
│  Patterns    │      │   Analysis   │
└──────┬───────┘      └──────┬───────┘
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│    Depth     │      │  Structure   │
│   Score      │      │   Score      │
│   (25%)      │      │   (15%)      │
└──────┬───────┘      └──────┬───────┘
       │                     │
       │              ┌──────┴───────┐
       │              │  Hedging     │
       │              │  Detection   │
       │              └──────┬───────┘
       │                     │
       │                     ▼
       │              ┌──────────────┐
       │              │ Confidence   │
       │              │   Score      │
       │              │   (15%)      │
       │              └──────┬───────┘
       │                     │
       └──────┬──────────────┘
              ▼
       ┌──────────────┐
       │   Weighted   │
       │  Turn Score  │
       │   (0-100)    │
       └──────┬───────┘
              │
              ▼
       ┌──────────────┐
       │ Check Trigger│
       │ relevance<50 │
       │  OR depth<60 │
       └──────┬───────┘
              │
       ┌──────┴──────┐
       │             │
       ▼             ▼
 ┌──────────┐  ┌──────────┐
 │ Follow-up│  │   Next   │
 │ Question │  │ Question │
 └──────────┘  └──────────┘
```

---

## 🧮 Evaluation Formula Tree

```
                     ┌─────────────┐
                     │ TURN SCORE  │
                     │   (0-100)   │
                     └──────┬──────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│   Clarity     │   │   Relevance   │   │     Depth     │
│   (× 0.20)    │   │   (× 0.25)    │   │   (× 0.25)    │
└───────┬───────┘   └───────┬───────┘   └───────┬───────┘
        │                   │                   │
        │                   │                   │
┌───────┴────────┐  ┌───────┴────────┐  ┌───────┴────────┐
│ Word Count:    │  │ Keyword Match: │  │ Has Example:   │
│ 30-200 = 85    │  │ matched/total  │  │ +20 if true    │
│ <30 = 40       │  │ × 100          │  │ Has Explain:   │
│ >200 = 60      │  │                │  │ +20 if true    │
└────────────────┘  └────────────────┘  │ Has Details:   │
                                        │ +10 if true    │
                                        │ Base: 50       │
                                        └────────────────┘

        ┌───────────────────┬───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│  Structure    │   │  Confidence   │   │               │
│   (× 0.15)    │   │   (× 0.15)    │   │               │
└───────┬───────┘   └───────┬───────┘   └───────────────┘
        │                   │
        │                   │
┌───────┴────────┐  ┌───────┴────────┐
│ Sentences ≥3:  │  │ Hedging Count: │
│ +25 if true    │  │ 100 - count×15 │
│ Has Intro:     │  │ max(0, score)  │
│ +12 if true    │  │                │
│ Has Conclusion:│  │ Patterns:      │
│ +13 if true    │  │ "I think"      │
│ Base: 50       │  │ "maybe"        │
└────────────────┘  │ "perhaps"      │
                    │ "probably"     │
                    └────────────────┘
```

---

## 🗄️ Database Schema

### ParsedResume Collection

```
{
  _id: ObjectId,
  userId: String (indexed),
  originalFile: {
    filename: String,
    mimeType: String,
    uploadDate: Date,
    fileSize: Number
  },
  rawText: String,
  parsedData: {
    contact: {
      name: String,
      email: String,
      phone: String,
      location: String,
      linkedin: String,
      github: String,
      portfolio: String
    },
    education: [{
      degree: String,
      major: String,
      institution: String,
      graduationYear: Number,
      gpa: Number,
      location: String
    }],
    experience: [{
      title: String,
      company: String,
      location: String,
      startDate: String,
      endDate: String,
      isCurrent: Boolean,
      duration: String,
      responsibilities: [String],
      achievements: [String]
    }],
    skills: {
      programming: [String],
      frameworks: [String],
      databases: [String],
      tools: [String],
      cloud: [String],
      softSkills: [String],
      other: [String]
    },
    projects: [{
      name: String,
      description: String,
      technologies: [String],
      link: String,
      startDate: String,
      endDate: String
    }],
    certifications: [{
      name: String,
      issuer: String,
      date: String,
      expiryDate: String,
      credentialId: String
    }],
    publications: [{
      title: String,
      venue: String,
      year: Number,
      authors: [String],
      link: String
    }]
  },
  atsScore: {
    totalScore: Number (0-100),
    componentScores: {
      skillsScore: Number,
      experienceScore: Number,
      educationScore: Number,
      structureScore: Number,
      keywordsScore: Number
    },
    scoreExplanation: {
      strengths: [String],
      weaknesses: [String],
      suggestions: [String]
    },
    formatAnalysis: {
      hasSections: Boolean,
      hasContactInfo: Boolean,
      hasExperience: Boolean,
      hasEducation: Boolean,
      hasSkills: Boolean,
      readabilityScore: Number,
      wordCount: Number
    },
    calculatedAt: Date
  },
  version: Number,
  isLatest: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

### ConversationalInterview Collection

```
{
  _id: ObjectId,
  userId: String (indexed),
  interviewType: String (hr|technical|behavioral|coding|mixed),
  targetRole: String,
  resumeId: ObjectId,
  jobDescription: {
    title: String,
    requiredSkills: [String],
    preferredSkills: [String],
    responsibilities: [String],
    rawText: String
  },
  status: String (scheduled|in-progress|completed|abandoned),
  turns: [{
    turnNumber: Number,
    question: {
      text: String,
      type: String,
      topic: String,
      difficulty: String (easy|medium|hard),
      expectedKeyPoints: [String],
      isFollowUp: Boolean,
      parentTurnNumber: Number
    },
    answer: {
      text: String,
      timestamp: Date,
      timeSpent: Number (seconds),
      wordCount: Number
    },
    evaluation: {
      clarity: Number (0-100),
      relevance: Number (0-100),
      depth: Number (0-100),
      structure: Number (0-100),
      confidence: Number (0-100),
      turnScore: Number (0-100),
      detectedKeyPoints: [String],
      missedKeyPoints: [String],
      feedback: String,
      needsFollowUp: Boolean,
      followUpReason: String
    }
  }],
  finalEvaluation: {
    scores: {
      technicalKnowledge: Number,
      communication: Number,
      problemSolving: Number,
      behavioral: Number,
      overall: Number
    },
    strengths: [String],
    weaknesses: [String],
    skillGaps: [{
      skill: String,
      gapType: String (knowledge-gap|explanation-gap|depth-gap),
      severity: String (high|medium|low),
      evidence: String
    }],
    readinessScore: Number (0-100),
    summary: String,
    recommendations: [String]
  },
  analytics: {
    totalTurns: Number,
    averageResponseTime: Number,
    averageTurnScore: Number,
    followUpCount: Number,
    topicsCovered: [String],
    difficultyCurve: [String]
  },
  interviewContext: {
    currentDifficulty: String,
    topicSequence: [String],
    performanceTrend: [Number],
    strugglingTopics: [String],
    strongTopics: [String]
  },
  createdAt: Date,
  completedAt: Date,
  updatedAt: Date
}
```

---

## 🔐 Security & Validation

### Input Validation

```
Resume Upload:
├─ File Type: PDF, DOC, DOCX only
├─ File Size: Max 5MB
├─ Content Scan: Text extraction validation
└─ User ID: Required, authenticated

Interview Start:
├─ User ID: Required, authenticated
├─ Interview Type: Enum validation
├─ Target Role: String, max 100 chars
└─ Resume ID: Optional, must exist if provided

Answer Submission:
├─ Interview ID: Must exist, status must be 'in-progress'
├─ Answer Text: Required, min 5 chars, max 5000 chars
├─ Time Spent: Optional, positive number
└─ User Ownership: Verify interview belongs to user
```

### Error Handling

```
Try-Catch Blocks:
├─ File Upload: Handle multer errors, file type errors
├─ Resume Parsing: Handle PDF extraction errors, text parsing errors
├─ Interview Evaluation: Handle missing fields, invalid data
├─ Database Operations: Handle connection errors, validation errors
└─ External APIs: Handle OpenAI API errors, rate limits

Error Responses:
├─ 400 Bad Request: Invalid input, missing fields
├─ 404 Not Found: Resource doesn't exist
├─ 500 Internal Error: Server-side failures
└─ Meaningful Messages: User-friendly error descriptions
```

---

This architecture demonstrates a well-structured, scalable system with clear separation of concerns, transparent evaluation methods, and robust error handling suitable for academic presentation and production deployment.
